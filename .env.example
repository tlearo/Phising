# Copy this file to `.env.local` and fill in the secrets before running locally.
# `.env` in the repo should remain placeholder-only to avoid leaking secrets.

NEON_DATABASE_URL=postgres://USER:PASSWORD@HOST/dbname?sslmode=require
ADMIN_SEED_TOKEN=replace-with-random-token
ADMIN_SEED_PASSWORD=replace-with-strong-password
BCRYPT_ROUNDS=12
